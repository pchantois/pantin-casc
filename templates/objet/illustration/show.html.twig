{% extends 'base.html.twig' %}

{% block title %}Illustration{% endblock %}

{% block body %}
    <h1>Illustration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ illustration.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ illustration.image }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ illustration.updatedAt ? illustration.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('objet_illustration_index') }}">back to list</a>

    <a href="{{ path('objet_illustration_edit', {'id': illustration.id}) }}">edit</a>

    {{ include('objet_illustration/_delete_form.html.twig') }}
{% endblock %}
